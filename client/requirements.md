## Packages
date-fns | Date formatting and manipulation for study schedules
recharts | Visualizing study progress and analytics
framer-motion | Smooth animations for UI transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- Syllabus upload uses standard FormData with 'file' field
- Assessment submission requires calculating time taken on client
- Plan generation assumes 'Medium' pace default until assessment overrides it
- Auth is handled via Replit Auth (useAuth hook already exists)
